<footer class="border-t border-slate-100 px-5 py-7 text-center">
    <nav class="mb-5 flex flex-wrap justify-center gap-3 text-[11px] font-semibold text-slate-500" aria-label="Liens de pied de page">
        <a href="{{ path('app_legal_privacy') }}" class="rounded-lg px-3 py-2 transition hover:bg-emerald-50 hover:text-emerald-700">Confidentialité</a>
        <a href="{{ path('app_legal_terms') }}" class="rounded-lg px-3 py-2 transition hover:bg-emerald-50 hover:text-emerald-700">CGU</a>
        <a href="{{ path('app_contact') }}" class="rounded-lg px-3 py-2 transition hover:bg-emerald-50 hover:text-emerald-700">Contact</a>
    </nav>
    <p class="text-sm font-black text-[#1A2B48]">Fit<span class="text-emerald-700">Progress</span></p>
    <p class="mt-2 text-[10px] text-slate-600">&copy; {{ 'now'|date('Y') }} FitProgress. Tous droits réservés.</p>
</footer>
